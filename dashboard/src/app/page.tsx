/**
 * @fileoverview Index page for the CCP dashboard.
 *
 * Placeholder landing while resource pages are built out.
 */

/**
 * Dashboard index - temporary placeholder.
 *
 * @returns {JSX.Element}
 */
export default function HomePage() {
  return (
    <main style={{ padding: 32, fontFamily: 'system-ui, sans-serif' }}>
      <h1 style={{ fontSize: 28, fontWeight: 600 }}>CCP Dashboard</h1>
      <p style={{ marginTop: 16, color: '#555' }}>
        The dashboard is starting up. Resource pages (projects, tasks, conversations)
        will land here as the build progresses.
      </p>
    </main>
  )
}
